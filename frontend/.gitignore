# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# vite-plugin-pwa's dev-mode service worker output (devOptions.enabled,
# see vite.config.ts) — generated on every `vite dev`/`vite build`, same
# spirit as dist/ above, not something to lint or commit.
dev-dist

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
